#include <bits/stdc++.h>
#define ll long long

using namespace std;

ll a, b;

int main() {
    cin.tie(NULL);
    ios::sync_with_stdio(false);
    cin >> a >> b;
    a == b ? cout << 1 : cout << 0;
    return 0;
}