#include <bits/stdc++.h>
#define ll long long

using namespace std;

int a;

int main() {
    cin.tie(NULL);
    ios::sync_with_stdio(false);
    cin >> a;
    if (a / 10000 == 555)   cout << "YES";
    else    cout << "NO"; 
    return 0;
}