#include <bits/stdc++.h>
#define ll long long

using namespace std;

int a, b, n, cnt = 0;

int main() {
    cin.tie(NULL);
    ios::sync_with_stdio(false);
    cin >> n;
    if (n)
        cout << "Leading the Way to the Future";
    else
        cout << "YONSEI";
    return 0;
}