#include <bits/stdc++.h>
#define ll long long

using namespace std;

int a, b, n, cnt = 0;

int main() {
    cin.tie(NULL);
    ios::sync_with_stdio(false);
    cin >> a >> b;
    cout << b-a << " " << b;
    return 0;
}