#include <bits/stdc++.h>
#define ll long long

using namespace std;

int l, a, b, c, d;
string s;

int main() {
    cin.tie(NULL);
    ios::sync_with_stdio(false);
    cin >> a;
    for (int i = 1; i <= a; i++)
        cout << "Hello World, Judge " << i << "!\n";
    return 0;
}