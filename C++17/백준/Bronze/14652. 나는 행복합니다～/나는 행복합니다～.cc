#include <bits/stdc++.h>
#define ll long long

using namespace std;

int a, b, c, d;

int main() {
    cin.tie(NULL);
    ios::sync_with_stdio(false);
    cin >> a >> b >> c;
    cout << c / b << " " << c % b;
    return 0;
}