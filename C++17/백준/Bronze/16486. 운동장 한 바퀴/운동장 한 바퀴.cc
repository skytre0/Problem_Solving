#include <bits/stdc++.h>
#define ll long long

using namespace std;

int a, b, c;

int main() {
    cin.tie(NULL);
    ios::sync_with_stdio(false);
    float pi = 3.141592;
    cin >> a >> b;
    cout << fixed << setprecision(6) << 2 * a + 2 * pi * b;
    return 0;
}