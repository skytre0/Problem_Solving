#include <bits/stdc++.h>
#define ll long long

using namespace std;

ll a;

int main() {
    cin.tie(NULL);
    ios::sync_with_stdio(false);
    cin >> a;
    cout << a - 1946;
    return 0;
}